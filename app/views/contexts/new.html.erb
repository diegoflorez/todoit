<%= form_for @context, url: {action: "create"},
  html: {class: "ui form"} do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <%= f.submit "New", :class=> 'ui green button' %>
<% end %>
